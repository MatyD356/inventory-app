extends layout

block content
  ul(class='list-none')
    each category in categories
      li(class='bg-white shadow-2xl m-4 p-4 rounded-md')
        h2(class='text-2xl text-red-900') #{category.name}