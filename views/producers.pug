extends layout

block content
  ul
    each producer in data
      a(href=producer.url)
        li(class='bg-white shadow-2xl m-4 p-4 rounded-md')
          h2(class='text-2xl text-red-900') #{producer.name}